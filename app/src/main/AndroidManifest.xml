<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.guvyerhopkins.livefront">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiveFront">
        <activity
            android:name="com.guvyerhopkins.livefront.ui.search.SearchActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.LiveFront.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.guvyerhopkins.livefront.ui.detail.ImageDetailActivity"
            android:parentActivityName=".ui.search.SearchActivity" />
    </application>

</manifest>